{
  "name": "ai-council",
  "version": "5.1.6",
  "private": true,
  "scripts": {
    "test": "npm run test:syntax && npm run test:e2e",
    "test:syntax": "node --check app.js && node --check sw.js",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "latest"
  }
}
